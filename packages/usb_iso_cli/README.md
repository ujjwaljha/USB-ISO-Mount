# usb_iso_cli

Command-line entry point for USB ISO Mount.

```bash
dart run usb_iso_cli list
dart run usb_iso_cli make --iso Win11.iso --disk disk4 --dry-run
```

See the [repository README](../../README.md).
